import { PriceonratePipe } from './priceonrate.pipe';

describe('PriceonratePipe', () => {
  it('create an instance', () => {
    const pipe = new PriceonratePipe();
    expect(pipe).toBeTruthy();
  });
});
